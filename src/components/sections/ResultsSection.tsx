const ResultsSection = () => (
  <section className="section-padding bg-primary-light">
    <div className="container max-w-4xl text-center">
      <blockquote className="text-2xl md:text-3xl font-heading font-medium text-primary leading-relaxed">
        "Cada web que entregamos está configurada para medir, optimizar y crecer — desde el día uno."
      </blockquote>
    </div>
  </section>
);

export default ResultsSection;
